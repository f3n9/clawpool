provider = "oidc"
oidc_issuer_url = "${KEYCLOAK_ISSUER_URL}"
redirect_url = "https://${OPENCLAW_HOST}/oauth2/callback"
http_address = "0.0.0.0:4180"

email_domains = [ "*" ]
upstreams = [ "http://traefik:80" ]

set_xauthrequest = true
pass_access_token = true
pass_authorization_header = true
pass_user_headers = true
scope = "openid profile email"

# Keycloak custom claim forwarding
oidc_groups_claim = "groups"
preferred_username_claim = "preferred_username"

# Use headers consumed by instance-manager
set_authorization_header = true
skip_provider_button = true
