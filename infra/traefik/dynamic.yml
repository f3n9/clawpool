http:
  routers:
    openclaw:
      rule: "Host(`claw.hatch.yinxiang.com`) && PathPrefix(`/`)"
      entryPoints:
        - web
      service: oauth2-proxy

  services:
    oauth2-proxy:
      loadBalancer:
        servers:
          - url: "http://oauth2-proxy:4180"
